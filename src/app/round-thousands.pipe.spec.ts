import { RoundThousandsPipe } from './round-thousands.pipe';

describe('RoundThousandsPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundThousandsPipe();
    expect(pipe).toBeTruthy();
  });
});
