import { MinutesToDHMPipe } from './minutes-to-dhm.pipe';

describe('MinutesToDHMPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesToDHMPipe();
    expect(pipe).toBeTruthy();
  });
});
